<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kang.chan_ce">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chance">
        <activity
            android:name=".MoreActivity"
            android:exported="true" />
        <activity
            android:name=".SignupActivity"
            android:exported="true" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <activity android:name=".SearchActivity" android:label="검색 페이지"/>
        <activity android:name=".StoreDetailActivity" android:label="스토어 상세 페이지"/>
        <activity android:name=".SubscriptionActivity" android:label="구독 설정 페이지"/>
        <activity android:name=".ReceiptActivity" android:label="결제 페이지"/>
        <activity android:name=".AccountActivity" android:label="입금 페이지"/>
        <activity android:name=".MypageActivity" android:label="마이 페이지"/>

    </application>

</manifest>